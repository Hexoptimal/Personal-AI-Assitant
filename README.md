# Personal-AI-Assitant
This repo contains config file for replicating the Personal AI Assistant

- Simply copy the docker-config.yaml file that is in the ollama-openui folder.
- Additionally you can copy the NGINX reverse proxy docker compose to spin up an instance if required.
